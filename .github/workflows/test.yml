name: My Test GA
on: 
  pull_request:
    branches: [test-*]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a github event."
      - run: echo "🍏 This job's status is done."
